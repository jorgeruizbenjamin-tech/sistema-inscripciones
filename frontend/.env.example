# Solo necesario en despliegues donde el frontend y el backend NO comparten
# el mismo origen (ej. Render, Railway). En desarrollo local y en
# docker-compose, deja esta variable sin definir: se usa el proxy "/api".
#
# VITE_API_URL=https://tu-backend-desplegado.onrender.com/api
